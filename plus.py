# \n, \t, \\ \, \'
# replace()
# [:] [start : ][ : end] [start : end : step]
# len(string), string.split(arguments)
# string.join(list), string.replace(A, B) A -> B
# r or l, strip(' ', '\t', '\n') 양쪽 없앰. '인수' -> 제거
# string.startswith('all') -> all로 시작하는가 T or F
# string.endswith
# string.find(), string.index() -> 몇 번째에 위치하는지 찾기
# r, l 입력으로 처음부터 or 끝부터 찾기 가능
# st.capitalize(), .title(), .upper(), .lower(), swapcase()
# .center(길이), .ljust(), rjust()
# format_string % data
# %s %d %x %o %f %e %g %%
